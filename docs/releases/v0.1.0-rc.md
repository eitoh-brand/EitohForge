# EitohForge v0.1.0-rc Preparation

Release candidate preparation status for `v0.1.0-rc`.

## Included Scope

- Enterprise security controls (tenant isolation, hardening middleware)
- SSO broker and OIDC/SAML baseline adapters
- Phase 18: Multi-tenant engine foundations (tenant context, cache/storage namespacing, tenant-aware SSO link mapping, optional Postgres `search_path` schema isolation)
- Plugin registry + feature flag staged rollout API
- Performance baseline + regression check tooling

## Validation Gates

- Ruff lint: pass
- Mypy type checks: pass
- Pytest suite: pass
- Packaging build (`python -m build`): pass
- pip check / twine metadata validation: pass
- Generated project integration test: pass

## Release Candidate Notes

- Performance baseline artifacts generated in `docs/performance/`
- Cookbook and usage guidance consolidated in `docs/guides/cookbook.md`
- Execution board reflects P14 closure

## Tag Intent

When repository tagging is enabled in CI/release workflow, create:

- `v0.1.0-rc`
