# EitohForge v0.1.4 Release Notes

Documentation-only packaging release: README structure and operational guidance. No intended SDK/runtime breaking changes.

## Highlights

- **How to read this README**: clarifies main guide vs appendix (inlined `docs/guides/` for PyPI single-file reading — intentional, not duplicate by mistake).
- **REST API & Feature Toggle Matrix**: compact tables for platform HTTP/WebSocket routes (`build_forge_app`), middleware outcomes, and `ForgePlatformToggles` / `wire_*` behavior.
- **New deployment targets (UAT, staging, custom)**: distinguishes operational new clusters/namespaces (no code change; map UAT → `staging` when useful) from adding a new `EITOHFORGE_APP_ENV` enum value (requires SDK/template changes).
- PyPI quick navigation and table of contents updated for the new sections.

## Validation

- `python -m build` + `twine check dist/*`: pass (recommended before publish).

## Upgrade notes

- No migration required for application code; upgrade if you want the updated PyPI long description.
